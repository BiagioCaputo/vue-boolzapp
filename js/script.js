console.log("Vue ok", Vue)

const {createApp} = Vue;

const app = createApp({
    name:"Boolzapp",
    data: () => ({
    }),
     
    computed: {
    },

    methods: {

        
    }

});

app.mount('#root');